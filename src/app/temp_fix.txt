        // Show errors if any
        if (errors.length > 0) {
          alert(`❌ Validation Error!\n\nCEO-level positions (tanpa Manager) harus punya Department UNIQUE:\n\n${errors.join('\n')}\n\nNote: Department harus beda dengan CEO-level positions lain (OK untuk sama dengan subordinates).`);
          toast.error(`${errors.length} validation error(s) - check console`);
          console.error('Validation errors:', errors);
          return; // Stop save process
        }
