
  # HeatMap Org Chart (new IDP)

  This is a code bundle for HeatMap Org Chart (new IDP). The original project is available at https://www.figma.com/design/9xbn6S01YKEci8eoW2hsdD/HeatMap-Org-Chart--new-IDP-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

  Test
  
